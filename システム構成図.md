# LineMap Engine GTFS Visual Platform
## システム構成・設計方針

最終更新: 2026-09-06

GTFS静的データとGTFS-RT Vehicle Positionsを、ブラウザ上の地図に重ねて表示するWebアプリケーションです。現在の実装は、FastAPIをAPI・静的ファイル配信の入口とし、PythonでGTFSをGeoJSONへ変換し、MapLibre GL JSで地図レイヤーとして描画します。

---

## 1. システム構成

```text
┌──────────────────────────────────────────────────────────────┐
│                      ユーザー（ブラウザ）                    │
│  HTML / CSS / JavaScript / MapLibre GL JS                    │
│  - 路線検索・停留所検索                                      │
│  - レイヤー表示設定・往復統合                                │
│  - GTFSアップロード・GTFS切替                               │
│  - PNGエクスポート                                           │
└──────────────────────────┬───────────────────────────────────┘
                           │ HTTP / JSON / GeoJSON
                           ▼
┌──────────────────────────────────────────────────────────────┐
│                         FastAPI                              │
│  app/main.py                                                 │
│  - 静的ファイル配信                                          │
│  - GTFSアップロード・選択API                                 │
│  - GeoJSON取得API                                            │
│  - GTFS-RT設定・車両状態API                                  │
│  - ヘルスチェック                                             │
└───────────────┬──────────────────────────────┬───────────────┘
                │                              │
                ▼                              ▼
┌───────────────────────────┐      ┌───────────────────────────┐
│ GTFS処理                  │      │ GTFS-RTサービス           │
│ app/gtfs.py               │      │ app/rt.py                 │
│ - ZIP内ファイル探索        │      │ - 定期ポーリング           │
│ - 文字コード判定           │      │ - protobufデコード         │
│ - routes / stops解析       │      │ - Vehicle Positions抽出    │
│ - shapes解析               │      │ - trip_id→route_id補完     │
│ - stop_connections生成     │      │ - 成功・エラー状態管理     │
│ - GeoJSON生成              │      └──────────────┬────────────┘
└───────────────┬───────────┘                     │ HTTPS
                │                                  ▼
                ▼                    ┌───────────────────────────┐
┌───────────────────────────┐         │ 外部GTFS-RT配信元          │
│ DataStore                  │         │ Vehicle Positions          │
│ app/storage.py             │         └───────────────────────────┘
│ - 複数GTFSをメモリ管理     │
│ - 現在選択データを保持     │
│ - 現在データをSQLite保存   │
└───────────────┬───────────┘
                ▼
┌───────────────────────────┐
│ data/linemap.db            │
│ SQLite blobs               │
│ GeoJSON / route catalog等  │
└───────────────────────────┘
```

### コンポーネントの責務

| コンポーネント | 主な責務 |
|---|---|
| `static/index.html` | 画面の入力欄、操作ボタン、地図コンテナの定義 |
| `static/app.js` | API呼び出し、MapLibreレイヤー、検索、表示設定、状態保存 |
| `static/styles.css` | 左パネル、地図領域、進捗表示、レスポンシブ表示 |
| `app/main.py` | FastAPI、APIルーティング、静的ファイル配信、起動時タスク管理 |
| `app/gtfs.py` | GTFS ZIPの読み込み、正規化、GeoJSON生成 |
| `app/rt.py` | GTFS-RT取得、protobuf解析、車両FeatureCollection、状態管理 |
| `app/storage.py` | 現在データの管理、複数GTFS切替、SQLite保存 |
| `data/linemap.db` | 現在選択中のGTFS由来データの保存先 |

---

## 2. 使用技術

- Python 3.11以降を推奨
- FastAPI 0.116.1
- Uvicorn 0.35.0
- MapLibre GL JS 4.7.1
- OpenFreeMap（Liberty / Bright / Positron）
- GeoJSON
- SQLite
- GTFS-RT protobuf（`gtfs-realtime-bindings`）
- `httpx` によるGTFS-RT HTTP取得

---

## 3. データフロー

### 3.1 GTFS静的データ

```text
GTFS ZIP
  │
  ├─ routes.txt
  ├─ trips.txt
  ├─ shapes.txt（任意）
  ├─ stops.txt
  └─ stop_times.txt（任意）
  │
  ▼
parse_gtfs_zip()
  │
  ├─ routes_geojson
  ├─ stops_geojson
  ├─ stop_connections_geojson
  ├─ route_catalog
  └─ trip_to_route
  │
  ▼
DataStore.update_gtfs()
  │
  ├─ メモリ上の現在データを更新
  ├─ gtfs_id単位のデータを保持
  └─ 現在データをSQLiteへ保存
  │
  ▼
FastAPI API
  │
  ▼
MapLibreのGeoJSON source / layer
```

必須ファイルは `routes.txt`、`trips.txt`、`stops.txt` です。ZIP直下だけでなく、ZIP内のサブフォルダーからもファイル名を探索します。テキストはUTF-8、CP932、Shift_JISの順で読み込みを試みます。

### 3.2 GTFS-RT

```text
FastAPI lifespan
  │
  └─ asyncio.create_task(polling_loop())
       │
       ├─ 設定済みURLへ定期HTTP GET
       ├─ FeedMessageをprotobufデコード
       ├─ vehicle.positionをPoint Featureへ変換
       ├─ trip_idからroute_idを補完
       └─ 成功・失敗・HTTPステータスを記録
            │
            ▼
         GET /vehicles
            │
            ▼
         車両GeoJSONをMapLibreへ反映
```

更新間隔は5～60秒に制限され、初期値は10秒です。設定がない場合は取得処理を実行せず、空の車両FeatureCollectionを返します。

---

## 4. GTFS処理設計

### 4.1 路線形状の優先順位

路線の表示形状は、次の優先順位で生成します。

1. `shapes.txt` のshape点列を利用
2. shapeとtrip、routeの紐付けを利用
3. `stop_times.txt` の停留所順序を利用
4. 停留所座標を直線で結ぶフォールバックを利用

これにより、`shapes.txt` が存在しないGTFSや、shape情報が不足したGTFSでも、表示可能な範囲で路線を描画します。

### 4.2 停留所連結線

停留所連結線は、tripごとの停留所順序から生成します。shapeが利用可能な場合はshape上に停留所を投影して経路に沿った線を生成し、shapeが利用できない場合は停留所座標を順番に結びます。

重複除外は、単純な `shape_id` 単位ではなく、route、shapeまたはtrip代替キー、停留所列を考慮します。これにより、複数の便が同じshapeを共有するGTFSでも連結線が過度に欠落しないようにします。

### 4.3 停留所情報

停留所Featureには、停留所名、停留所コード、親停留所などの情報を保持します。親停留所の情報しか表示名がない場合は、親停留所をたどって表示名を補完します。

停留所ポップアップでは、可能な範囲で通過路線と簡易時刻情報を表示します。

### 4.4 路線色

- `route_color` が6桁の16進数として有効なら利用
- 未設定または不正な値の場合は `route_id` から決定的に色を生成
- 同じ `route_id` は同じ自動色になる

---

## 5. フロントエンド表示設計

### 5.1 MapLibreレイヤー

| レイヤー | 種類 | 用途 |
|---|---|---|
| `routes-line` | line | 路線表示 |
| `routes-line-hit` | line | 路線クリック用の透明な当たり判定 |
| `stops-circle` | circle | 停留所表示 |
| `stop-connections-line` | line | 停留所連結線表示 |
| `stop-connections-line-hit` | line | 停留所連結線クリック用の当たり判定 |
| `vehicles-symbol` | symbol | 車両矢印と車両情報表示 |

可視状態は `state.showStops`、`state.showStopConnections`、`state.showVehicles` で管理し、`applyDisplaySettings()` から各レイヤーへ一括反映します。

### 5.2 表示設定

表示設定には次を含みます。

- 路線ごとの表示対象
- 停留所の表示状態
- 停留所連結線の表示状態
- 車両の表示状態
- 往復路線の統合状態
- 統合時の採用カラー
- 停留所連結線の線幅
- ベースマップスタイル

表示設定は `linemap:state`、地図の中心・ズームは `linemap:view` としてlocalStorageへ保存します。

### 5.3 往復路線の統合

統合表示は、route_short_nameとroute_long_nameの情報を利用して候補を判定します。発地・着地の「発・行き」表記や矢印表記を正規化し、往復関係を判定します。

統合時は、代表路線のカラーを採用し、必要に応じて中間線を生成します。判定できない表記では同一short_nameを候補として扱うため、運用データによっては誤統合の可能性があります。

統合表示中は、停留所連結線の表示を抑制します。これは、統合後の路線線と元の連結線が重複して見えることを避けるためです。

---

## 6. API設計

### システム

| Method | Path | 内容 |
|---|---|---|
| `GET` | `/` | Web画面 |
| `GET` | `/health` | ヘルスチェック |

### GTFS

| Method | Path | 内容 |
|---|---|---|
| `POST` | `/upload` | GTFS ZIPを登録 |
| `POST` | `/upload_gtfs` | `/upload`の互換エンドポイント |
| `GET` | `/gtfs_list` | 登録済みGTFS一覧 |
| `POST` | `/gtfs/select` | 表示対象GTFSを切替 |
| `GET` | `/routes` | 路線GeoJSON |
| `GET` | `/route_catalog` | 路線一覧 |
| `GET` | `/stops` | 停留所GeoJSON |
| `GET` | `/stop_connections` | 停留所連結線GeoJSON |

アップロードは `gtfs_id` クエリを受け付けます。レスポンスには、登録ID、路線数、停留所数、停留所連結線数を含みます。

### GTFS-RT

| Method | Path | 内容 |
|---|---|---|
| `GET` | `/vehicles` | 車両FeatureCollectionと取得状態 |
| `POST` | `/settings/gtfs_rt` | URLと更新間隔を設定 |

GTFS-RT設定の入力例:

```json
{
  "gtfs_rt_url": "https://example.com/vehiclePositions.pb",
  "interval_sec": 10
}
```

---

## 7. 設計方針

### 7.1 責務を分離する

- `main.py`: HTTP APIとアプリケーションライフサイクル
- `gtfs.py`: GTFSの解析と静的データ生成
- `rt.py`: GTFS-RTの取得と車両状態
- `storage.py`: データの保持と切替
- `static/app.js`: ブラウザ状態と地図描画

データ処理をAPIハンドラーや画面コードに直接埋め込まず、入力形式の変更や表示方式の変更が他の層へ波及しにくい構成にします。

### 7.2 入力データの欠損に耐える

GTFSは配布元によって、文字コード、ZIP内階層、shapeの有無、停留所名の持ち方が異なります。そのため、単一の正常系だけを前提にせず、次を基本方針とします。

- ファイル名はZIP内のbasenameで探索する
- 複数の文字コードを順に試す
- 欠損値を安全に補完する
- shapeがなければ停留所座標へフォールバックする
- 不正な座標や不正な色は対象を限定して処理を継続する

### 7.3 正規化したデータを境界にする

バックエンドからフロントエンドへ渡すデータは、地図で扱いやすいGeoJSONとします。フロントエンドはGTFSのCSV構造を直接解釈せず、路線・停留所・連結線・車両のFeatureCollectionを描画する責務に限定します。

### 7.4 表示状態は一元管理する

レイヤーの表示切替を各イベントハンドラーで個別に実装せず、状態変更後に共通の表示反映処理を呼び出します。これにより、初期表示、GTFS切替、ベースマップ変更、ユーザー操作で表示状態が食い違うことを防ぎます。

### 7.5 GTFS静的データとGTFS-RTを分離する

GTFS静的データは路線・停留所・経路の基礎情報、GTFS-RTは車両の変動情報として扱います。RT取得失敗時も静的地図を利用できるよう、RTの失敗をGTFS解析や地図初期化の失敗に波及させません。

### 7.6 状態を可観測にする

GTFS-RTでは最終更新時刻、最終成功時刻、最終エラー、HTTPステータス、連続エラー回数、エラー履歴を保持します。表示切替や統合表示ではデバッグ情報を画面に表示し、データ件数やレイヤー状態を確認できるようにします。

### 7.7 互換性を維持する

既存利用者やスクリプトから呼ばれる可能性があるため、GTFS登録の `/upload` と `/upload_gtfs` は併存させます。APIを変更する場合は、既存レスポンスの意味を維持し、必要な追加情報はフィールド追加で対応します。

---

## 8. 永続化と複数GTFSの方針

現在のDataStoreは次の二層構造です。

1. **メモリ**: 複数の `gtfs_id` と表示中データを高速に保持
2. **SQLite**: 現在選択中のGeoJSON、路線カタログ、tripとrouteの対応を保存

現在は、複数GTFSそれぞれの内容を再起動後も復元する永続化までは行っていません。将来、GTFS管理を本格化する場合は次の拡張を行います。

- `gtfs_feeds` テーブルの追加
- GTFSごとのメタデータ、登録時刻、ファイルハッシュの保存
- GTFSごとのGeoJSONまたは元ZIPの保存
- アプリ起動時の登録GTFS復元
- GTFS削除・再登録・更新履歴の管理

---

## 9. エラー処理方針

| 場面 | 方針 |
|---|---|
| ZIP以外のアップロード | HTTP 400で拒否 |
| 必須GTFSファイル不足 | HTTP 400で内容を返す |
| GTFS-RT HTTPエラー | 状態へ記録し、前回データまたは空データで画面を維持 |
| GTFS-RT protobuf解析エラー | エラー状態を表示し、ポーリングを継続 |
| 存在しないGTFS選択 | HTTP 404で返す |
| 不正な座標・色 | 該当データをスキップまたは自動補完 |
| ベースマップ切替 | style再読込後にオーバーレイを再生成 |

エラーの詳細はサーバーログと `/vehicles` のstatus、ブラウザの開発者ツールで確認します。

---

## 10. セキュリティ・運用上の注意

- 現在のCORS設定は開発用に全オリジンを許可しています。外部公開時は許可オリジンを限定します。
- アップロードされたZIPは信頼できるデータを前提としています。公開運用ではファイルサイズ、展開後サイズ、処理時間の制限を追加します。
- GTFS-RT URLは外部通信先になるため、公開運用では接続先の許可リストやSSRF対策を検討します。
- SQLiteのDBファイルはアプリの作業ディレクトリに依存するため、本番運用では明示的なデータディレクトリを設定します。
- OpenFreeMapの利用条件、タイル制限、可用性を確認して運用します。

---

## 11. テスト・検証方針

### 自動テスト

往復路線統合判定のテストケースを `data/route_merge_testcases.json` で管理します。

```powershell
.\.venv\Scripts\python.exe tests\test_route_merge.py
```

### 実データ検証

GTFSを登録した後、次を確認します。

1. `/health` が `{"status":"ok"}` を返す
2. アップロード結果の路線数・停留所数が妥当である
3. `/routes`、`/stops`、`/stop_connections` のFeature数を確認する
4. 路線一覧、停留所検索、レイヤー切替を操作する
5. 往復統合のON/OFFで線形状と色を確認する
6. GTFS-RT設定後に `/vehicles` のstatusを確認する
7. 大規模データでは表示切替の応答時間を計測する

### 性能計測

`static/performance-monitor.js` を読み込んだ画面では、ブラウザコンソールから次を実行できます。

```javascript
PerformanceMonitor.measureStopConnectionToggle()
PerformanceMonitor.measureStopsToggle()
PerformanceMonitor.help()
```

---

## 12. 既知の制約

- 往復路線の判定は、route名の表記ゆれに影響されます。
- 統合表示の中間線は幾何学的な近似であり、実道路の中心線と完全には一致しない場合があります。
- GTFS-RTはVehicle Positionsのみを対象とし、Trip UpdatesやService Alertsは対象外です。
- 複数GTFSの一覧と内容は、現時点ではアプリ再起動後に完全復元されません。
- 大規模GTFSでは、停留所連結線のFeature数とブラウザ描画負荷が増加します。
- 外部ベースマップとGTFS-RT配信元の可用性に依存します。

---

## 13. 今後の拡張方針

### 優先度高

- 複数GTFSの永続化と管理画面
- アップロードファイルサイズ・処理時間の制限
- APIテストとGTFS解析の単体テスト拡充
- GTFS読み込み結果の検証レポート化

### 優先度中

- 停留所ポップアップの通過便・時刻情報の拡充
- GTFS-RTのTrip Updates、Service Alerts対応
- 路線統合判定ルールの設定化
- 大規模データ向けの簡略化、分割表示、遅延ロード

### 優先度低

- 鉄道など他の交通モードへの拡張
- 複数都市・複数事業者の比較表示
- DBスキーマの正規化と更新履歴
- PNG以外のGeoJSON、CSV、画像出力

---

## 14. 関連ドキュメント

- [README](README.md)
- [複数GTFS設計](docs/multi_gtfs_design.md)
- [停留所検索設計](docs/stop_search_design.md)
- [計画と課題](まとめる.md)
