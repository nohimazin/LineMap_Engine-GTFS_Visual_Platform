# ■ システム構成図（Python版）

```
[ユーザー（ブラウザ）]
        │
        │ HTTPアクセス
        ▼
[Python Webサーバー]
（FastAPI / Flask）
        │
        ├─ 静的ファイル配信（HTML / JS / CSS）
        │
        ├─ API
        │   ├─ /upload_gtfs
        │   ├─ /routes
        │   ├─ /stops
        │   ├─ /vehicles (RT)
        │
        ▼
[データ処理層（Python）]
        │
        ├─ GTFSパーサ
        ├─ GeoJSON変換
        ├─ 路線色処理
        ├─ キャッシュ
        │
        ▼
[データストア]
（メモリ or SQLite）
        │
        ▼
[GTFS-RT取得処理]
（定期実行）
        │
        ▼
[外部GTFS-RT API]
```

---

# ■ 使用技術（確定版）

* バックエンド：Python（FastAPI推奨）
* 地図表示：MapLibre GL JS
* ベースマップ：OpenFreeMap（Liberty / Bright / Positron 切替）
* データ形式：GeoJSON
* DB：SQLite（任意）

---

# ■ 要件定義（Python前提）

## 1. 目的

GTFSおよびGTFS-RTを用いて、
**実際の地図上にバス路線・停留所・車両位置を可視化するシステム**

---

## 2. 機能要件

### 2.1 データ入力

* GTFS ZIPアップロード
* GTFS-RT URL設定
* データ再読み込み機能

---

### 2.2 地図表示

* 実地図（ベクタータイル）
* 以下を重ねる：

  * 路線（線）
  * 停留所連結線（停留所同士を結ぶ線）
  * 停留所（点）
  * 車両（アイコン）

---

### 2.3 路線表示

* route_id単位で表示
* 色分け：

  * `route_color` 使用
  * 未設定 → 自動生成
* ON/OFF切替
* クリックで詳細表示
* 往復路線の統合表示オプション
* 統合時カラー選択（route_idが小さい側 / 大きい側）

---

### 2.4 停留所表示

* 全停留所表示
* クリックで：

  * 停留所名
  * 通過路線
  * 時刻（簡易）

---

### 2.5 リアルタイム（GTFS-RT）

* 車両位置表示
* 5〜15秒更新
* trip_idで路線紐付け

---

### 2.6 UI（超重要）

* シンプル設計

  * 左：路線一覧
  * 中央：地図
* 検索：

  * 路線名
  * 停留所名
* ワンクリック操作
* ベースマップスタイル切替（OpenFreeMap複数スタイル）
* 停留所連結線 ON/OFF 切替

---

### 2.7 出力

* 地図画像（PNG）
* 表示状態をそのまま保存

---

## 3. データ処理要件

### 3.1 GTFS静的処理

Pythonで実施：

入力：

* routes.txt
* trips.txt
* shapes.txt
* stops.txt
* stop_times.txt

出力：

* routes.geojson（線）
* stops.geojson（点）
* stop_connections.geojson（停留所連結線）

処理内容：

* shape_id → route_id紐付け
* 線の統合
* 色付け
* trip_id + stop_times による停留所順序の復元
* shapesベースで停留所連結線を生成（shapes未存在時は直線補間）
* ZIP内サブフォルダ配置のGTFSファイル読込対応

---

### 3.2 GTFS-RT処理

* protobufをデコード
* vehicle位置抽出
* JSON化

---

## 4. API設計（シンプル）

* `/upload` / `/upload_gtfs` → GTFS登録
* `/routes` → 路線GeoJSON
* `/route_catalog` → 路線一覧
* `/stops` → 停留所GeoJSON
* `/stop_connections` → 停留所連結線GeoJSON
* `/vehicles` → 車両位置
* `/settings/gtfs_rt` → GTFS-RT URL・更新間隔設定
* `/health` → ヘルスチェック

---

## 5. 非機能要件

### 操作性

* 非エンジニアでも使える
* インストール不要（ブラウザ）

---

### パフォーマンス

* 初回表示：5秒以内
* 更新：リアルタイム

---

### 拡張性

* 複数都市対応
* 鉄道追加可能

---

## 6. 開発ステップ

① GTFS読み込み（Python）
② GeoJSON生成
③ MapLibreで表示
④ 色分け実装
⑤ 停留所表示
⑥ GTFS-RT追加
⑦ UI改善

---

## 7. 実装済み追加仕様（2026-04追記）

* OpenFreeMapの複数スタイル切替に対応（表示状態をローカル保存）
* 停留所連結線クリック時に路線名を表示
* 路線・停留所連結線のクリック当たり判定を拡大（可視線と分離）
* 往復統合表示時、マッチした2路線の中間線を1本生成して描画
* モバイル幅で左メニュー全体をスクロール可能化し、配置崩れを修正

