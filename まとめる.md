# 現在の計画・今後の予定・課題点

最終更新: 2026-09-06

## TL;DR

- 状態: GTFS静的データの地図表示、GTFS-RT車両表示、複数GTFS切替、停留所検索、表示設定まで実装済み。
- 検証済み: 往復路線統合判定テスト6/6 PASS、停留所検索テスト8/8 PASS、GTFSパース性能の実測。
- 現在の重点: 実データでの安定性確認、GTFS管理機能の永続化、起動・API・ブラウザ操作の一連の検証。
- 次の大きな機能: 複数GTFSの永続管理、削除・更新、DBスキーマ整理。

## 1. 現在の実装状態

### 完了

- [x] FastAPIによるWebサーバーと静的ファイル配信
- [x] GTFS ZIPアップロード
- [x] ZIP内サブフォルダーのGTFSファイル探索
- [x] UTF-8、CP932、Shift_JISの読み込み対応
- [x] `routes.txt`、`trips.txt`、`stops.txt`の必須チェック
- [x] `shapes.txt`を利用した路線形状生成
- [x] shape情報が不足する場合の停留所連結線フォールバック
- [x] 路線色の正規化と未設定色の自動生成
- [x] 路線、停留所、停留所連結線のGeoJSON生成
- [x] 停留所ポップアップの路線・簡易時刻情報
- [x] 路線検索とroute_id単位の表示切替
- [x] 停留所名検索と検索結果からの地図移動
- [x] GTFS-RT Vehicle Positionsのprotobuf取得・デコード
- [x] bearing対応の車両矢印表示
- [x] 車両ポップアップとRTエラー状態表示
- [x] OpenFreeMapの3スタイル切替
- [x] 停留所、車両、停留所連結線の表示切替
- [x] 停留所連結線の線幅変更
- [x] 往復路線の統合表示と統合時カラー選択
- [x] 地図表示のPNG保存
- [x] 表示設定と地図位置のlocalStorage保存
- [x] GTFS読み込み進捗表示
- [x] 複数GTFSのメモリ上の登録・切替
- [x] ルート統合判定テスト6/6 PASS
- [x] 停留所検索テスト8/8 PASS
- [x] 表示切替性能計測ツール
- [x] READMEとシステム構成・設計方針の更新

### 実装の主要ファイル

| ファイル | 役割 |
|---|---|
| `app/main.py` | FastAPI、API、静的配信、起動時RTポーリング管理 |
| `app/gtfs.py` | GTFS解析、正規化、GeoJSON生成 |
| `app/rt.py` | GTFS-RT取得、protobuf解析、車両状態管理 |
| `app/storage.py` | 複数GTFSのメモリ管理、現在データのSQLite保存 |
| `static/app.js` | 地図表示、検索、表示状態、API連携 |
| `static/index.html` | UI構造 |
| `static/styles.css` | UIとレスポンシブ表示 |
| `static/performance-monitor.js` | 表示切替性能計測 |

## 2. 現在の設計方針

### 2.1 責務分離

GTFS解析、GTFS-RT取得、データ保持、API、地図表示を分離します。CSVやprotobufの解釈をフロントエンドへ持ち込まず、ブラウザにはGeoJSONと表示用メタデータを渡します。

### 2.2 欠損データへのフォールバック

GTFSは配布元によって文字コード、ZIP構成、shape情報、停留所名の持ち方が異なります。そのため、単一の正常系を前提にせず、次の順で処理します。

1. 入力ファイルを柔軟に探索する
2. 複数の文字コードを試す
3. 欠損する表示名や色を補完する
4. shapeがなければ停留所連結線へフォールバックする
5. 不正な個別データはスキップして処理を継続する

### 2.3 表示状態の一元管理

表示設定はstateに保持し、設定変更後に共通の表示反映処理を実行します。初期読み込み、GTFS切替、ベースマップ変更、ユーザー操作のすべてで同じ反映経路を使うことを基本とします。

### 2.4 静的データとリアルタイムデータの分離

GTFSは路線・停留所・経路の基礎情報、GTFS-RTは変動する車両情報として扱います。RT配信元が停止しても、静的地図と検索機能は利用できる設計にします。

### 2.5 観測可能性

RTの最終更新時刻、最終成功時刻、最終エラー、HTTPステータス、連続失敗回数、エラー履歴を保持します。統合表示ではレイヤー状態やFeature件数を確認できるようにします。

## 3. 現在のデータ保持方針

現在のDataStoreは次の構成です。

- メモリ: `gtfs_id`ごとの複数GTFSと現在選択中のデータ
- SQLite: 現在選択中のGeoJSON、route catalog、tripとrouteの対応
- GTFS-RT: サービスインスタンスのメモリ上に車両と状態を保持

複数GTFSは現時点ではメモリ管理が中心です。アプリ再起動後に、登録済みGTFS一覧や各GTFSの内容を完全復元する仕組みは未実装です。

## 4. 現在の課題

### 4.1 GTFS管理の永続化

- `gtfs_id`単位のデータをSQLiteへ完全保存できていない
- GTFSの削除、名前変更、再登録、更新履歴がない
- 登録済みGTFSのファイルハッシュや登録日時を管理していない

### 4.2 往復路線の統合精度

- route名の表記ゆれに依存する
- 同じshort_nameの別系統を誤統合する可能性がある
- 循環線や複雑な経由表記では発地・着地の判定が難しい
- 統合時の中間線は幾何学的近似であり、実道路形状と一致しない場合がある

### 4.3 大規模GTFSの描画負荷

- 停留所連結線が多いフィードではGeoJSONとMapLibreの描画負荷が高くなる
- GTFS解析と初回描画に時間がかかる
- 現在はタイル化、空間インデックス、遅延ロードを行っていない

### 4.4 実データ運用の検証範囲

- 自動テストは統合判定と停留所検索が中心
- APIのアップロード、GTFS切替、RT取得を含むエンドツーエンドテストが不足している
- ブラウザ上の表示設定は実データ・ブラウザ環境ごとの確認が必要

### 4.5 運用・セキュリティ

- CORSは開発用に全オリジン許可
- アップロードサイズ、展開後サイズ、処理時間の制限が未実装
- GTFS-RT URLは任意の外部URLへ接続するため、公開運用時はSSRF対策が必要
- OpenFreeMapと外部RT配信元の可用性に依存する

### 4.6 UI/UX

- 小画面では設定項目と路線一覧の情報密度が高い
- 表示設定が増えたため、設定のグループ化や初期値の見直しが必要
- エラー表示と読み込み進捗の表示場所をさらに整理できる

## 5. 今後の実装計画

### Phase 3: GTFS管理機能

優先度: 高

- [ ] `gtfs_feeds`テーブルを追加する
- [ ] GTFSごとの登録日時、表示名、ファイルハッシュ、件数を保存する
- [ ] GTFSごとのデータを再起動後に復元する
- [ ] GTFS削除APIを追加する
- [ ] GTFS再登録・更新APIを追加する
- [ ] UIに削除、更新、登録状態を追加する
- [ ] 現在選択中のGTFSと保存状態を明確に表示する

### Phase 4: API・検証強化

優先度: 高

- [ ] `/health`以外の主要APIをテストする
- [ ] 小さなGTFS ZIPを使ったアップロードテストを追加する
- [ ] GTFS切替のAPIテストを追加する
- [ ] GTFS-RT成功、HTTPエラー、protobufエラーをテストする
- [ ] 解析結果の件数と欠損情報を検証レポートとして返す
- [ ] 起動コマンドと実際の起動確認をCIまたはスクリプトに組み込む

### Phase 5: 性能・描画改善

優先度: 中

- [ ] 実データごとの解析時間とFeature数を計測する
- [ ] stop_connectionsの重複削減と簡略化を検討する
- [ ] 路線・停留所の表示範囲による遅延ロードを検討する
- [ ] 大規模データではベクトルタイルまたはサーバー側分割を検討する
- [ ] 停留所検索にインデックスまたは検索APIを導入する

### Phase 6: GTFS-RT拡張

優先度: 中

- [ ] Trip Updates対応
- [ ] Service Alerts対応
- [ ] 車両の運行遅延・運行状態表示
- [ ] RTフィードの認証・ヘッダー設定
- [ ] フィードごとの更新時刻とデータ鮮度の表示

### Phase 7: UI・出力改善

優先度: 低

- [ ] 表示設定パネルの整理
- [ ] モバイル操作性の改善
- [ ] PNG出力品質と出力範囲の改善
- [ ] GeoJSON、CSVなどのエクスポート追加
- [ ] 路線・停留所の詳細画面追加

## 6. 直近の作業順

1. 開発環境でFastAPIが起動することを確認する
2. `/health`、トップ画面、静的ファイル配信を確認する
3. 小規模GTFSでアップロードから地図表示まで確認する
4. 路線・停留所・連結線・車両の各表示切替を確認する
5. 往復統合ON/OFFとGTFS切替を確認する
6. GTFS-RTの成功・失敗状態を確認する
7. その結果をAPIテストと実データ検証手順へ反映する
8. GTFS管理の永続化設計を確定する

## 7. 検証コマンド

### 往復路線統合テスト

```powershell
.\.venv\Scripts\python.exe tests\test_route_merge.py
```

### ブラウザ上の性能計測

```javascript
PerformanceMonitor.measureStopConnectionToggle()
PerformanceMonitor.measureStopsToggle()
PerformanceMonitor.help()
```

### 起動

```powershell
uvicorn app.main:app --reload
```

ブラウザで `http://127.0.0.1:8000` を開き、次を確認します。

- GTFSアップロードと読み込み進捗
- GTFS選択と切替
- 路線検索・停留所検索
- 表示設定とレイヤー切替
- GTFS-RTステータス
- PNG出力

## 8. 完了条件

次の状態になった時点で、現行基盤を安定版として扱います。

- [ ] 開発環境で起動エラーがない
- [ ] 小規模GTFSのアップロードから表示まで成功する
- [ ] 複数GTFSの登録・切替が再現可能である
- [ ] 主要APIの自動テストがある
- [ ] レイヤー表示設定のブラウザ検証が完了している
- [ ] RT成功・失敗時の画面表示が確認済みである
- [ ] 大規模GTFSの性能基準を実測している
- [ ] GTFS管理の永続化方針が決定している

## 9. 関連ドキュメント

- [README](README.md)
- [システム構成・設計方針](システム構成図.md)
- [複数GTFS設計](docs/multi_gtfs_design.md)
- [停留所検索設計](docs/stop_search_design.md)
