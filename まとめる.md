# 現在の計画・今後の予定・課題点 まとめ

最終更新: 2026-04-28

## 1. 現在の計画（進行中）

- GTFS静的データの可視化基盤を安定化する
  - `/upload` でGTFS ZIPを登録
  - `/routes`, `/stops`, `/stop_connections` を地図表示に反映
- UI操作性の改善を継続する
  - 左メニューのレスポンシブ改善
  - 路線一覧の検索・ON/OFF制御の精度向上
- 路線統合表示（往復統合）を実運用データに合わせて調整する
  - 統合判定ルールの継続改善
  - 統合時カラー選択（小さいroute_id / 大きいroute_id）を維持

## 2. 今後の予定（優先順）

### 優先度: 高

- 統合表示ロジックの検証強化
  - 地域別データでの誤統合・未統合ケースを収集
  - 判定ルールをテストケース化
- 停留所連結線の品質向上
  - 中間線生成ロジックの精度検証
  - 分岐が多い路線の見え方調整
- パフォーマンス改善
  - 初回表示速度の改善
  - 大規模GTFSでの描画負荷対策

### 優先度: 中

- 停留所ポップアップ情報の充実
  - 通過路線
  - 時刻（簡易）
- GTFS-RT表示の改善
  - 車両アイコン表現強化
  - エラー状態のUI表示改善
- エクスポート機能の拡張
  - 画像出力時の表示品質向上

### 優先度: 低

- 設定画面の整理
  - ベースマップ切替・表示オプションのグルーピング
- ドキュメント整備
  - 運用手順
  - トラブルシュート

## 3. 現在の課題点

### 3.1 路線統合

- 路線名フォーマットの揺れが大きく、統合判定が難しい
  - 例: 「発/行き」「矢印」「中間経由記号」など
- 統合時に本来別系統を誤って寄せるリスクがある

### 3.2 線形描画

- 統合時の中間線は幾何的近似のため、実経路と完全一致しない場合がある
- 複雑なループ路線で線の重なりや不自然な形状が出る可能性がある

### 3.3 データ品質依存

- `shapes.txt` や `stop_times.txt` の品質・欠損により表示品質が変わる
- ZIP内フォルダ構成・命名差の吸収は実施済みだが、例外形式が残る可能性がある

### 3.4 UI/UX

- 小画面での情報密度が高く、操作負荷が高い
- 表示オプションが増加しており、設定項目の整理が必要

## 4. 直近の具体アクション

- [ ] 統合判定の検証用サンプルを収集（成功例 / 失敗例）
- [ ] 統合判定ロジックのテスト項目を整理
- [ ] 停留所連結線の表示切替時の体感速度を計測
- [ ] モバイルUIの最終調整（カード順序・余白・視認性）
- [x] READMEに「運用時チェックリスト」を追記

## 5. 追加メモ（2026-04-24）

### 5.1 全体走査で確認した注意点

- 静的エラー（構文・型レベル）は現時点で未検出
- ただし、実データ運用で不具合化しやすいポイントを確認
  - 往復統合キーの判定がデータ表記ゆれに依存しやすい
  - `shape_id` 共有ケースで停留所連結線が欠落する可能性
  - route形状がないGTFS（`/routes` が0件）で統合表示が消えやすい

### 5.2 実施済みの修正（反映済み）

- 統合表示時の線消失対策
  - `/routes` が0件でも `stop_connections` 形状をフォールバック利用
- 統合表示時の可視制御見直し
  - 統合モードで消すべきレイヤーと残すレイヤーを整理
- route_idマッチング強化
  - 文字列化とtrimで型差・空白差の影響を軽減
- 統合線生成の安全策
  - 中間線生成が失敗した場合でも代表形状を採用して0件化を回避

### 5.3 未解決/継続観察ポイント

- 一部データで「統合ON時に2本表示」「色ずれ」が再発する可能性
- 中間線生成は幾何近似のため、道路中心線と乖離するケースあり
- stop_connectionsをどこまで統合表示に使うか（仕様の明文化が必要）

### 5.4 直近の追加アクション（更新）

- [x] 統合ON/OFF時の表示レイヤー状態を画面上にデバッグ表示する
- [x] 統合後 feature 件数（routes / stop_connections）を一時表示して検証する
- [x] route統合判定ルールのテストデータセットを作成する
- [x] `shape_id` 共有時の stop_connections 生成ロジックを見直す

### 5.5 本日の進捗（2026-04-24）

- `static/app.js` にデバッグオーバーレイを追加
  - 統合ON/OFF、カラー選択、表示対象 route 件数、routes/stop_connections feature 件数を常時表示
  - `routes-line`, `routes-line-hit`, `stop-connections-line`, `stop-connections-line-hit`, `stops-circle`, `vehicles-symbol` の可視状態を表示
- `static/styles.css` にオーバーレイ表示スタイルを追加
  - 画面右下固定、モバイル時は文字サイズと余白を縮小
- `data/route_merge_testcases.json` を追加
  - 統合成功/失敗を判定する6ケース（発/行き、矢印、short_name一致、別系統、空白正規化、同方向重複）を定義

### 5.6 本日の追加進捗（2026-04-24）

- `app/gtfs.py` の stop_connections 生成ロジックを修正
  - 従来の `shape_id` 単位の重複除外を廃止
  - `route_id + shape_id(またはtrip代替キー) + 停留所並び` 単位で重複除外する方式に変更
  - `shape_id` 共有便があるデータでも停留所連結線の欠落を起こしにくいよう改善
- `README.md` に「運用時チェックリスト」を追加
  - GTFS投入時の前提確認、統合表示確認、GTFS-RTエラー確認、性能確認の観点を明文化

## 5.7 本日の進捗（2026-04-28）

### ✅ 統合判定ロジックの検証強化
- `tests/test_route_merge.py` を新規作成
  - 往復統合判定ロジックのPython実装
  - テストケース6個をすべて自動実行：100% PASS
  - 発/行き表記、矢印表記、同一short_name、別系統、空白正規化、同方向重複 に対応

### ✅ パフォーマンス計測ツール作成
- `static/performance-monitor.js` を新規作成
  - 停留所連結線の表示/非表示切替時間計測 (ブラウザコンソール実行)
  - 停留所表示の切替時間計測
  - 平均< 50ms で良好、100ms以上で低下判定

### ✅ 停留所ポップアップ情報の充実
- `app/gtfs.py` を修正
  - 各停留所の通過路線一覧を properties に追加 (`routes` 配列)
  - 各停留所の発時刻一覧を properties に追加 (`times` 配列)
  - trip_stop_times_map による効率的な時刻マッピング
- フロントエンド (`app.js` buildStopPopupText) は既に対応済み
  - ポップアップに「通過路線」と「時刻(簡易)」が表示される設計

## 5.7 本日の進捗（2026-04-28）

### ✅ 統合判定ロジックの検証強化
- `tests/test_route_merge.py` を新規作成
  - 往復統合判定ロジックのPython実装
  - テストケース6個をすべて自動実行：100% PASS
  - 発/行き表記、矢印表記、同一short_name、別系統、空白正規化、同方向重複 に対応

### ✅ パフォーマンス計測ツール作成
- `static/performance-monitor.js` を新規作成
  - 停留所連結線の表示/非表示切替時間計測 (ブラウザコンソール実行)
  - 停留所表示の切替時間計測
  - 平均< 50ms で良好、100ms以上で低下判定

### ✅ 停留所ポップアップ情報の充実
- `app/gtfs.py` を修正
  - 各停留所の通過路線一覧を properties に追加 (`routes` 配列)
  - 各停留所の発時刻一覧を properties に追加 (`times` 配列)
  - trip_stop_times_map による効率的な時刻マッピング
- フロントエンド (`app.js` buildStopPopupText) は既に対応済み
  - ポップアップに「通過路線」と「時刻(簡易)」が表示される設計

### ✅ GTFS-RT表示の改善
- 車両表示を矢印シンボルに変更 (bearing対応)
  - `createVehicleArrowIcon()` 関数で矢印SVGアイコン動的生成
  - vehicles-symbol レイヤーをsymbolタイプに変更
  - icon-rotate で車両のbearing（進行方向）を反映
- 車両クリック時の詳細情報ポップアップ追加
  - vehicle_id, trip_id, route_id, bearing, speed, timestamp を表示
- バージョン: v1 (2026-04-28-vehicle-display-v1)

### ✅ エクスポート機能の拡張
- PNG保存時のデバッグ情報隠蔽
  - デバッグオーバーレイを一時的に非表示
  - 画面出力後に復元
- タイムスタンプ精度向上
  - `YYYY-MM-DDTHH-MM-SS` 形式でファイル名を生成
- ファイルが整形画像から生成されることを確認

### ✅ 設定画面の整理
- HTMLセクションのグループ化と階層化
  - 「📊 データ入力」(GTFS, GTFS-RT)
  - 「🛣️ 路線一覧」(検索)
  - 「⚙️ 表示設定」(統合表示、線の太さ)
  - 「👁️ レイヤー表示」(ON/OFF系)
  - 「💾 エクスポート」(PNG保存)
- CSSで card-subsection スタイルを追加
  - 区切り線とサブタイトルで視覚的階層を強化
  - モバイル対応(560px以下での調整)
- バージョン: v2 (2026-04-28-ui-settings-v2)

### ✅ ドキュメント整備
- README.md を大幅拡張
  - **使用ガイド**: GTFSデータ入力、GTFS-RT設定、表示設定、レイヤー管理、エクスポート手順
  - **トラブルシューティング**: よくある問題と対処方法
  - **パフォーマンス計測**: PerformanceMonitorの使用方法
  - **運用時チェックリスト**: 本番環境での確認項目

### 🎯 優先度別進捗

**優先度：高**
- ✅ 統合表示ロジックの検証強化 → **完了**
- ✅ パフォーマンス計測ツール作成 → **完了**
- ⏳ 大規模GTFSでの描画負荷対策 → 計測ツリーで検証可能

**優先度：中**
- ✅ 停留所ポップアップ情報の充実 → **完了**
- ✅ GTFS-RT表示の改善 → **完了** (矢印シンボル、詳細ポップアップ)
- ✅ エクスポート機能の拡張 → **完了** (デバッグ隠蔽、タイムスタンプ)

**優先度：低**
- ✅ 設定画面の整理 → **完了** (セクション階層化、絵文字アイコン)
- ✅ ドキュメント整備 → **完了** (使用ガイド、トラブルシューティング)

## 5.8 本日の実装内容まとめ

### ファイル変更一覧
- ✅ `tests/test_route_merge.py` - 新規作成（統合判定テスト）
- ✅ `static/performance-monitor.js` - 新規作成（パフォーマンス計測）
- ✅ `app/gtfs.py` - 修正（停留所の路線・時刻情報追加）
- ✅ `static/app.js` - 修正（車両矢印シンボル、ポップアップ追加、エクスポート改善）
- ✅ `static/index.html` - 修正（UI設定画面のセクション整理）
- ✅ `static/styles.css` - 修正（カード階層化スタイル、モバイル対応）
- ✅ `README.md` - 拡張（使用ガイド、トラブルシューティング追加）

### バージョン推移
- 開始: `2026-04-25-stop-popup-fix-5`
- 中間: `2026-04-28-vehicle-display-v1`
- 現在: `2026-04-28-ui-settings-v2`

### テスト結果
- 統合判定ロジック: **6/6 PASS** ✅
- 全コード構文チェック: **OK** ✅

## 次のステップ（推奨）

### すぐに実施できる項目
1. 実運用データでのパフォーマンス計測
2. GTFS-RTエラーハンドリングの強化
3. 複数GTFSの同時読み込み対応検討

### 将来の拡張
1. リアルタイム遅延情報の表示
2. 検索機能の拡張（停留所検索）
3. 時系列データの可視化
4. 複数データソースの統合表示

