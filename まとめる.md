# 現在の計画・今後の予定・課題点 まとめ

最終更新: 2026-05-02

## TL;DR

- 状態: 複数 GTFS 対応の Phase 1/2 と停留所検索 MVP まで完了。
- 動作確認: GTFS の切り替え、停留所検索、RT 車両表示まで確認済み。
- 次の作業: Phase 3 の GTFS 管理機能、または DB スキーマ更新。

## 1. 現在の計画（進行中）

- GTFS静的データの可視化基盤を安定運用する
- UI操作性を維持しつつ、必要な機能だけを追加する
- 実データで問題が出る箇所を小さく検証して改善する

## 2. 今後の予定（優先順）

### 優先度: 高

- ✅ 複数 GTFS 同時読み込み対応 → 完了
- ✅ 停留所検索 MVP → 完了
- Phase 3 の GTFS 管理機能の検討
- DB スキーマ更新（`gtfs_id` カラム追加）の検討

### 優先度: 中

- 停留所ポップアップ情報の充実
  - 通過路線
  - 時刻（簡易）
- GTFS-RT表示の改善
  - 車両アイコン表現強化
  - エラー状態のUI表示改善
- エクスポート機能の拡張
  - 画像出力時の表示品質向上

### 優先度: 低

- 設定画面の整理
- ドキュメント整備

## 3. 現在の課題点

### 3.1 路線統合

- 路線名フォーマットの揺れが大きく、統合判定が難しい
  - 例: 「発/行き」「矢印」「中間経由記号」など
- 統合時に本来別系統を誤って寄せるリスクがある

### 3.2 線形描画

- 統合時の中間線は幾何的近似のため、実経路と完全一致しない場合がある
- 複雑なループ路線で線の重なりや不自然な形状が出る可能性がある

### 3.3 データ品質依存

- `shapes.txt` や `stop_times.txt` の品質・欠損により表示品質が変わる
- ZIP内フォルダ構成・命名差の吸収は実施済みだが、例外形式が残る可能性がある

### 3.4 UI/UX

- 小画面での情報密度が高く、操作負荷が高い
- 表示オプションが増加しており、設定項目の整理が必要

## 4. 直近の具体アクション

- [ ] Phase 3 の GTFS 管理機能を設計する
- [ ] DB スキーマ更新の要否を決める
- [x] 停留所連結線の表示切替時の体感速度を計測
- [x] READMEに「運用時チェックリスト」を追記

## 5. 追加メモ（2026-04-24）

### 5.1 全体走査で確認した注意点

- 静的エラー（構文・型レベル）は現時点で未検出
- ただし、実データ運用で不具合化しやすいポイントを確認
  - 往復統合キーの判定がデータ表記ゆれに依存しやすい
  - `shape_id` 共有ケースで停留所連結線が欠落する可能性
  - route形状がないGTFS（`/routes` が0件）で統合表示が消えやすい

### 5.2 実施済みの修正（反映済み）

- 統合表示時の線消失対策
  - `/routes` が0件でも `stop_connections` 形状をフォールバック利用
- 統合表示時の可視制御見直し
  - 統合モードで消すべきレイヤーと残すレイヤーを整理
- route_idマッチング強化
  - 文字列化とtrimで型差・空白差の影響を軽減
- 統合線生成の安全策
  - 中間線生成が失敗した場合でも代表形状を採用して0件化を回避

### 5.3 未解決/継続観察ポイント

- 一部データで「統合ON時に2本表示」「色ずれ」が再発する可能性
- 中間線生成は幾何近似のため、道路中心線と乖離するケースあり
- stop_connectionsをどこまで統合表示に使うか（仕様の明文化が必要）

### 5.4 直近の追加アクション（更新）

- [x] 統合ON/OFF時の表示レイヤー状態を画面上にデバッグ表示する
- [x] 統合後 feature 件数（routes / stop_connections）を一時表示して検証する
- [x] route統合判定ルールのテストデータセットを作成する
- [x] `shape_id` 共有時の stop_connections 生成ロジックを見直す

### 5.5 本日の進捗（2026-04-24）

- `static/app.js` にデバッグオーバーレイを追加
  - 統合ON/OFF、カラー選択、表示対象 route 件数、routes/stop_connections feature 件数を常時表示
  - `routes-line`, `routes-line-hit`, `stop-connections-line`, `stop-connections-line-hit`, `stops-circle`, `vehicles-symbol` の可視状態を表示
- `static/styles.css` にオーバーレイ表示スタイルを追加
  - 画面右下固定、モバイル時は文字サイズと余白を縮小
- `data/route_merge_testcases.json` を追加
  - 統合成功/失敗を判定する6ケース（発/行き、矢印、short_name一致、別系統、空白正規化、同方向重複）を定義

### 5.6 本日の追加進捗（2026-04-24）

- `app/gtfs.py` の stop_connections 生成ロジックを修正
  - 従来の `shape_id` 単位の重複除外を廃止
  - `route_id + shape_id(またはtrip代替キー) + 停留所並び` 単位で重複除外する方式に変更
  - `shape_id` 共有便があるデータでも停留所連結線の欠落を起こしにくいよう改善
- `README.md` に「運用時チェックリスト」を追加
  - GTFS投入時の前提確認、統合表示確認、GTFS-RTエラー確認、性能確認の観点を明文化

## 5.7 本日の進捗（2026-04-28）

### 実装した内容
- `tests/test_route_merge.py` を新規作成し、往復統合判定ロジックを6ケースで検証
  - 発/行き表記、矢印表記、同一 short_name、別系統、空白正規化、同方向重複に対応
- `static/performance-monitor.js` を新規作成し、表示切替の計測ツールを追加
  - 停留所連結線と停留所表示の切替時間をブラウザコンソールから計測可能
- `app/gtfs.py` を修正し、停留所ポップアップ情報を充実
  - `routes` 配列と `times` 配列を追加し、通過路線と簡易時刻を表示可能にした
- `static/app.js` を修正し、GTFS-RT表示を改善
  - 車両を bearing 対応の矢印シンボルで表示
  - 車両クリック時に詳細情報を表示
- `static/index.html` と `static/styles.css` を整理し、設定画面を階層化
  - データ入力、路線一覧、表示設定、レイヤー表示、エクスポートを分割
- `README.md` を拡張し、使用ガイドとトラブルシューティングを追加

### 追加修正
- `static/app.js` の RT ステータス表示を状態付きに整理
  - 最終更新時刻、ポーリング間隔、接続設定有無を表示
  - エラー時は `data-state="error"` で強調表示
- `static/performance-monitor.js` をブラウザコンソールでそのまま使える形に修正
  - `globalThis.PerformanceMonitor` として公開
  - 矢印アイコン生成の例外を修正

### 検証結果
- 統合判定ロジック: **6/6 PASS** ✅
- 全コード構文チェック: **OK** ✅

### バージョン推移
- 開始: `2026-04-25-stop-popup-fix-5`
- 中間: `2026-04-28-vehicle-display-v1`
- 現在: `2026-04-28-ui-settings-v2`

### 優先度別進捗

**優先度：高**
- ✅ 統合表示ロジックの検証強化 → **完了**
- ✅ パフォーマンス計測ツール作成 → **完了**
- ⏳ 大規模GTFSでの描画負荷対策 → 計測ツリーで検証可能

**優先度：中**
- ✅ 停留所ポップアップ情報の充実 → **完了**
- ✅ GTFS-RT表示の改善 → **完了**
- ✅ エクスポート機能の拡張 → **完了**

**優先度：低**
- ✅ 設定画面の整理 → **完了**
- ✅ ドキュメント整備 → **完了**

## 次のステップ（推奨）

### すぐに実施できる項目
1. 実運用データでのパフォーマンス計測
2. GTFS-RTエラーハンドリングの強化
3. 複数GTFSの同時読み込み対応検討

## 5.8 本日の進捗（2026-04-30）

### 実測した内容
- `C:\Users\user\Downloads\sankobus` を入力にして GTFS パース性能を計測
  - 1回計測の結果: 約 6.233 秒
  - 生成件数: routes=0, stops=4612, stop_connections=14884
- RT フィード URL を確認
  - `https://km.bus-vision.jp/realtime/sankobus_vpos_update.bin`
  - `application/octet-stream` で応答し、サイズは 14809 bytes

### 追加修正
- `scripts/gtfs_perf.py` を拡張し、展開済み GTFS フォルダをそのまま計測できるようにした
- `app/rt.py` に GTFS-RT のエラー履歴・連続失敗回数・最終成功時刻を追加した
- `static/app.js` の RT ステータス表示を拡張し、エラー履歴と retry 情報を画面表示するようにした

### 検証結果
- `GET /vehicles` で `error_history` / `last_success_unix` / `consecutive_error_count` を確認済み

## 5.9 本日の追加進捗（2026-04-30）

### 実装した内容
- `docs/multi_gtfs_design.md` を追加し、複数 GTFS 対応の設計ドキュメントを整理
- `app/storage.py` を拡張し、複数 GTFS を同時保持する仕組みを実装
  - `_gtfs_data` に複数の GTFS を `gtfs_id` キーで保存
  - `_current_gtfs_id` で表示対象を制御
  - `list_gtfs()` / `select_gtfs()` メソッドを追加
- `app/main.py` に新規エンドポイントを追加
  - `GET /gtfs_list` - 登録済み GTFS 一覧取得
  - `POST /gtfs/select?gtfs_id=xxx` - 表示対象 GTFS を切り替え
  - アップロード時に `?gtfs_id=xxx` で複数 ID での保存に対応

### 実装ステータス
- Phase 1（バックエンド基盤）: **3/4 完了** ✅
  - DB スキーマの `gtfs_id` カラム追加は次回以降

### バージョン推移
- 前回: `2026-04-28-ui-settings-v2`
- 現在: `2026-04-30-multi-gtfs-v1`

## 5.10 本日の追加進捗（2026-04-30 完了）

### 実装した内容（複数 GTFS フェーズ 2）
- `static/index.html` に GTFS 選択ドロップダウンセクションを追加
- `static/app.js` に GTFS 選択機能を実装
  - `refreshGtfsList()` - `/gtfs_list` から登録済み GTFS を取得し、ドロップダウン補充
  - `selectGtfs()` - `/gtfs/select` で表示対象 GTFS を切り替え、マップ再読み込み
  - ページロード時に自動的に `refreshGtfsList()` を呼び出し
  - アップロード後に `refreshGtfsList()` で一覧を更新

### 実装したコンテンツ（停留所検索 Phase 1 - MVP）
- `static/index.html` に停留所検索セクションを追加
- `static/styles.css` に停留所検索リストのスタイルを追加
- `static/app.js` に停留所検索機能を実装
  - `performStopSearch()` - 停留所名で絞り込み検索（最大 30 件、大文字小文字区別なし）
  - `renderStopSearchResults()` - 結果をリストで表示（路線数表示付き）
  - `zoomToStop()` - クリック時にマップ中心を移動・ズーム（zoom: 15）
  - wireActions() にリアルタイム入力リスナーを追加
- `docs/stop_search_design.md` に設計ドキュメントを作成
- `tests/test_stop_search.html` に統合テストを作成

### 構文確認結果
- `static/app.js` - ✅ No errors found
- `static/index.html` - ✅ No errors found
- `static/styles.css` - ✅ No errors found

### テスト結果
- 停留所検索機能 - **8/8 PASS** ✅
  - 1 文字部分一致フィルタリング
  - 大文字小文字区別なし
  - 日本語検索対応
  - 空白入力時は空結果
  - 複数一致（2 件以上）
  - マッチなしの処理
  - Platform 検索

### 実装ステータス
- 複数 GTFS フェーズ 1（バックエンド基盤）: **完了** ✅
- 複数 GTFS フェーズ 2（フロントエンド UI）: **完了** ✅
- 停留所検索 MVP: **完了** ✅ → **テスト合格 8/8 PASS**

### 実装ファイル一覧
- `static/index.html` - UI セクション追加
- `static/app.js` - 検索ロジック + イベントハンドラー
- `static/styles.css` - スタイル定義
- `docs/stop_search_design.md` - 設計ドキュメント
- `tests/test_stop_search.html` - 統合テスト

### 次のステップ
- Phase 3（GTFS 削除・管理機能）
- または DB スキーマ更新（gtfs_id カラム追加）
- または他の優先度タスクへ

## 5.11 本日の追加進捗（2026-05-02）

### 実装した内容
- 複数 GTFS 対応の Phase 1/2 を完了
  - `app/storage.py` に複数 GTFS の保持・切り替え機能を追加
  - `app/main.py` に `GET /gtfs_list` と `POST /gtfs/select` を追加
  - `static/index.html` と `static/app.js` に GTFS 選択 UI を追加
- 停留所検索の MVP を追加
  - `static/index.html` に停留所検索セクションを追加
  - `static/app.js` に停留所名検索・結果表示・ズーム処理を追加
  - `static/styles.css` に検索結果リストのスタイルを追加
- 設計と検証を整理
  - `docs/stop_search_design.md` を作成
  - `tests/test_stop_search.html` で 8/8 PASS を確認

### 検証結果
- `static/app.js` - ✅ No errors found
- `static/index.html` - ✅ No errors found
- `static/styles.css` - ✅ No errors found
- 停留所検索テスト - ✅ 8/8 PASS

### 現在の状態
- 複数 GTFS の基本機能はフロントエンド・バックエンドともに動作確認済み
- 停留所検索は最小実装として完成
- 次の候補は Phase 3 の GTFS 管理機能、または DB スキーマ更新

